<Window x:Class="UzlePlugins.Views.HoleTaskView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:vm ="clr-namespace:UzlePlugins;assembly=UzlePlugins.Vm"
        xmlns:local="clr-namespace:UzlePlugins.Views"
        mc:Ignorable="d"
        Title="HoleTaskView" Height="450" Width="800">

    <Grid>
        <TabControl x:Name="products">
            <TabItem x:Name="smartphonesTab">
                <TabItem.Header>
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Margin="3">Holes/Отверстия</TextBlock>
                    </StackPanel>
                </TabItem.Header>
                <TabItem.Content>
                    <StackPanel>
                        <DataGrid Name="DG1" ItemsSource="{Binding HolesVm }" AutoGenerateColumns="False" >
                            <DataGrid.Columns>
                                <DataGridTextColumn Header="#"  Binding="{Binding FirstName}" />
                                <DataGridTextColumn Header="Intersection with" Binding="{Binding LastName}" />
                                <DataGridTextColumn Header="Figure" Binding="{Binding Email}" />
                                <DataGridTextColumn Header="Size" Binding="{Binding IsMember}" />
                                <DataGridTextColumn Header="Type of basis"/>
                                <DataGridTextColumn Header="Basis material"/>
                                <DataGridComboBoxColumn Header="Hole figure">
                                </DataGridComboBoxColumn>
                                <DataGridTextColumn Header="Offset">
                                </DataGridTextColumn>
                                <DataGridCheckBoxColumn Header="Insert/Вставка" />
                            </DataGrid.Columns>
                        </DataGrid>
                    </StackPanel>
                </TabItem.Content>
            </TabItem>
            <TabItem x:Name="tabletsTab">
                <TabItem.Header>
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Margin="3">Settings/Настройки</TextBlock>
                    </StackPanel>
                </TabItem.Header>
            </TabItem>
        </TabControl>
    </Grid>
</Window>
