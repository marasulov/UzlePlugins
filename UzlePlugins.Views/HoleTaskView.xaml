<Window x:Class="UzlePlugins.Views.HoleTaskView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:vm ="clr-namespace:UzlePlugins;assembly=UzlePlugins.Vm"
        xmlns:local="clr-namespace:UzlePlugins.Views"
        mc:Ignorable="d"
        Title="HoleTaskView" Height="450" Width="800">

    <Grid >
         <DockPanel>
     <GroupBox Header="New Holes" DockPanel.Dock="Top">
         <StackPanel Orientation="Horizontal">
             <TabControl x:Name="Holes" Width="Auto" Height="Auto">
                 <TabItem x:Name="holesTab">
                     <TabItem.Header>
                         <StackPanel Orientation="Horizontal">
                             <TextBlock Margin="3">Holes</TextBlock>
                         </StackPanel>
                     </TabItem.Header>
                     <TabItem.Content>
                         <StackPanel>
                             <DataGrid Name="NewHolesGrid"
                                ItemsSource="{Binding Holes}"
                                AutoGenerateColumns="False"
                                ScrollViewer.CanContentScroll="True" 
                                ScrollViewer.VerticalScrollBarVisibility="Visible"
                                ScrollViewer.HorizontalScrollBarVisibility="Auto">
                                 <DataGrid.Columns>
                                     <DataGridTextColumn Header="#"  Binding="{Binding Id}" />
                                     <DataGridTextColumn Header="Intersection with" Binding="{Binding IntersectionPoint}" />
                                     <DataGridTextColumn Header="Figure" Binding="{Binding HoleType}" />
                                     <DataGridTextColumn Header="Size" Binding="{Binding HoleSize}" />
                                     <DataGridTextColumn Header="Type of basis" Binding="{Binding HoleSourceType}"/>
                                     <DataGridTextColumn Header="Basis material" Binding="{Binding IsHoleCircled}"/>
                                     <DataGridComboBoxColumn Header="Hole figure" >
                                     </DataGridComboBoxColumn>
                                     <DataGridTextColumn Header="Offset" Binding="{Binding HoleOffset}"/>
                                     <DataGridCheckBoxColumn Header="Insert/Вставка" Binding="{Binding IsInsert}"/>
                                 </DataGrid.Columns>
                             </DataGrid>
                         </StackPanel>
                     </TabItem.Content>
                 </TabItem>
                 <TabItem x:Name="tabletsTab">
                     <TabItem.Header>
                         <StackPanel Orientation="Horizontal">
                             <TextBlock Margin="3">Settings</TextBlock>
                         </StackPanel>
                     </TabItem.Header>
                 </TabItem>
             </TabControl>
         </StackPanel>
     </GroupBox>

     <GroupBox Header="Existing " DockPanel.Dock="Top">
         <StackPanel Orientation="Horizontal">
             <DataGrid Width="800">
                 <DataGrid.Columns>
                     <DataGridTextColumn Header="Header1" Width="100" />
                     <DataGridTextColumn Header="Header2" Width="*" />
                     <DataGridTextColumn Header="Header3" Width="*" />
                     <DataGridTextColumn Header="Header4" Width="*" />
                 </DataGrid.Columns>
             </DataGrid>
         </StackPanel>
     </GroupBox>
  
     <StackPanel 
          
         HorizontalAlignment="Right" VerticalAlignment="Stretch">
          <Button 
              Name="OkButton" 
              Width="100" 
              Height="20" 
              Content="Ok" 
              Command="{Binding CreateHolesCommand}" 
              CommandParameter="Clicked"/>
     </StackPanel>
     <StackPanel 
         HorizontalAlignment="Right" 
         VerticalAlignment="Stretch" >
          <Button Name="CancelButton" Width="100" Height="20" Content="Cancel"   />
     </StackPanel>
 </DockPanel>

    </Grid>
</Window>
